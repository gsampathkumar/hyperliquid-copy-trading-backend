/**
 * Enum representing the deployment environments for the application.
 * Used for environment-specific configuration and behavior.
 */
export enum EnvironmentEnum {
  /** Production environment */
  prod = 'prod',
  /** Staging environment */
  stage = 'stage',
  /** Development environment */
  dev = 'dev',
}
